i've added the vscode option and new icons, use the current layout as default so maybe add the icons to the root dir of the plugin, then users can change with their own path, also linked the dir to a repository, edit the readme and push the final changes, include the installation, userflow, update and removal of the plugin with the default omarchy way and the quickshell will be triggered by a keybind (SUPER + Z), state that on the readme and ofc users can change it so one should unbind it and bind it with the plugin on the bindings.lua file